{>layout/}
{<content}
		<h2>Games_users</h2>
      <div class="form-group">
          <label for="game_id">game_id</label><br>
					<p id="game_id">{#rows}<a href="/games/{game_id}">{games_name}</a>{/rows}</p>
      </div>
      <div class="form-group">
          <label for="user_id">user_id</label><br>
					<p id="user_id">{#rows}<a href="/users/{user_id}">{users_name}</a>{/rows}</p>
      </div>
			<br>
{/content}
